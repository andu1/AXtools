% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crispr_plots.R
\name{plot_enrichment_barcode}
\alias{plot_enrichment_barcode}
\title{Per-gene barcode plot of sgRNA enrichment scores}
\usage{
plot_enrichment_barcode(
  sample.info,
  pos.samples = c("pos-1", "pos-2"),
  unsorted.sample = "unsorted-1",
  id.column = "sgID",
  sample.column = "Sample_ID",
  value.column = "cell_num",
  sg.pattern = "_sg[0-9]+$",
  n_genes = 20,
  log.score = TRUE,
  log.base = 10,
  min.unsorted = 1,
  bar.width = 0.7,
  bar.colour = "steelblue",
  median.colour = "firebrick",
  title = NULL,
  save.plot = FALSE,
  output.file = "enrichment_barcode.pdf",
  plot.width = 10,
  plot.height = 7
)
}
\arguments{
\item{sample.info}{Path to a CSV file or a data.frame with at least
columns for sgRNA id, sample id, and a numeric value.}

\item{pos.samples}{Character vector of exactly two positive sample names
(x-axis and y-axis).}

\item{unsorted.sample}{Name of the pre-sorted / unsorted denominator sample.}

\item{id.column}{Column name for the per-sgRNA identifier.}

\item{sample.column}{Column name holding sample names.}

\item{value.column}{Column name for the numeric value to correlate.}

\item{sg.pattern}{Regex stripped from sgID to extract the gene name.}

\item{n_genes}{Number of top genes to display (default 20).}

\item{log.score}{If \code{TRUE}, apply a log transform to the enrichment scores
before plotting and regression; guides with score <= 0 are dropped.
If \code{FALSE}, plot raw ratios.}

\item{log.base}{Base of the log transform when \code{log.score = TRUE} (default
10). Common choices: 10, 2, or \code{exp(1)} for natural log.}

\item{min.unsorted}{Minimum unsorted cell number to include a guide.}

\item{bar.width}{Width of individual sgRNA bars (default 0.7).}

\item{bar.colour}{Fill colour for the bars.}

\item{median.colour}{Colour of the median indicator line per gene.}

\item{title}{Plot title; auto-generated if \code{NULL}.}

\item{save.plot}{If \code{TRUE}, save to \code{output.file}.}

\item{output.file}{File path for saved plot.}

\item{plot.width, plot.height}{Dimensions in inches for saved plot.}
}
\value{
A list with \code{plot} (ggplot object) and \code{gene_scores} (data.frame
of per-gene median scores, sorted descending).
}
\description{
Shows the top genes ranked by median enrichment score, with each sgRNA
drawn as an individual bar so you can assess whether enrichment is
consistent across multiple guides or driven by a single outlier.
Enrichment is computed as sample cell number / unsorted cell number,
averaged across the supplied replicates.
}
