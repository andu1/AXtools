% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crispr_plots.R
\name{plot_low_abundance_venn}
\alias{plot_low_abundance_venn}
\title{Venn diagram of low-abundance sgRNAs across negative samples}
\usage{
plot_low_abundance_venn(
  sample.info,
  neg.samples = c("neg-1", "neg-2"),
  hit.samples = NULL,
  id.column = "sgID",
  sample.column = "Sample_ID",
  value.column = "cell_num",
  log.threshold = log10(2),
  include.zeros = TRUE,
  reference.sample = "unsorted-1",
  gene.level = FALSE,
  sg.pattern = "_sg[0-9]+$",
  annotate = "count",
  name.shared.min = NULL,
  name.max = 12,
  palette.name = "Set1",
  plot.title = NULL,
  save.plot = FALSE,
  output.file = "low_abundance_venn.pdf",
  plot.width = 7,
  plot.height = 6
)
}
\arguments{
\item{sample.info}{Path to a CSV file or a data.frame with at least
columns for sgRNA id, sample id, and a numeric value.}

\item{neg.samples}{Character vector of negative sample names.}

\item{hit.samples}{Optional character vector of positive samples whose
guides are overlaid on the Venn.}

\item{id.column}{Column name for the per-sgRNA identifier.}

\item{sample.column}{Column name holding sample names.}

\item{value.column}{Column name for the numeric value to correlate.}

\item{log.threshold}{sgRNAs with log10(cell_num) below this are counted as
low-abundance.}

\item{include.zeros}{If \code{TRUE}, sgRNAs absent from a negative but present
in \code{reference.sample} are counted as low-abundance (0 cells).}

\item{reference.sample}{Universe sample for the zero rule.}

\item{gene.level}{If \code{TRUE}, collapse sgRNAs to genes using \code{sg.pattern}.}

\item{sg.pattern}{Regex to strip the sgRNA suffix for gene-level analysis.}

\item{annotate}{\code{"count"} to show hit counts per Venn region, or \code{"none"}.}

\item{name.shared.min}{If set, table hits low in at least this many
negatives.}

\item{name.max}{Maximum rows in the corner table.}

\item{palette.name}{RColorBrewer palette name.}

\item{plot.title}{Plot title; auto-generated if \code{NULL}.}

\item{save.plot}{If \code{TRUE}, save to \code{output.file}.}

\item{output.file}{File path for saved plot.}

\item{plot.width, plot.height}{Dimensions in inches for saved plot.}
}
\value{
A list with \code{plot}, \code{sets}, and \code{hits} (invisibly).
}
\description{
Draws a Venn diagram of sgRNAs (or genes) whose cell number falls below a
threshold in each negative sample. Optionally overlays positive-hit
membership and prints a table of shared hits.
}
