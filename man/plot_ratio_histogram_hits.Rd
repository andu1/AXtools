% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crispr_plots.R
\name{plot_ratio_histogram_hits}
\alias{plot_ratio_histogram_hits}
\title{Ratio histogram with corresponding positive hits and statistics}
\usage{
plot_ratio_histogram_hits(
  sample.info,
  presorted.sample = "unsorted-1",
  neg.samples = c("neg-1", "neg-2"),
  hit.map = NULL,
  spike.sample = NULL,
  id.column = "sgID",
  sample.column = "Sample_ID",
  show.stats = TRUE,
  value.column = "cell_num",
  log.ratio = TRUE,
  z.tail = "lower",
  bold.quantile = 0.05,
  label.markers = NULL,
  marker.colour = "black",
  bins = 50,
  list.size = 2.6,
  stat.size = 2.9,
  point.size = 1.9,
  spike.colour = "gray35",
  spike.shape = 17,
  spike.size = 2.3,
  label.mode = "both",
  palette.name = "Set1",
  hist.fill = "grey80",
  hist.colour = "grey45",
  plot.title = NULL,
  save.plot = FALSE,
  output.file = "ratio_histogram_hits.pdf",
  plot.width = 11,
  plot.height = 7,
  include.zeros = TRUE,
  zero.x = -3,
  zero.bar = TRUE
)
}
\arguments{
\item{sample.info}{Path to a CSV file or a data.frame with at least
columns for sgRNA id, sample id, and a numeric value.}

\item{presorted.sample}{Name of the denominator sample.}

\item{neg.samples}{Character vector of negative sample names.}

\item{hit.map}{Named character vector mapping neg -> pos samples.}

\item{spike.sample}{Optional spike-in sample; \code{NULL} to disable.}

\item{id.column}{Column name for the per-sgRNA identifier.}

\item{sample.column}{Column name holding sample names.}

\item{show.stats}{If \code{TRUE}, annotate per-panel statistics.}

\item{value.column}{Column name for the numeric value to correlate.}

\item{log.ratio}{If \code{TRUE}, x-axis is log10(ratio).}

\item{z.tail}{\code{"lower"} for one-sided or \code{"two"} for two-sided p-value.}

\item{bold.quantile}{Bottom quantile of the ratio distribution to bold.}

\item{label.markers}{sgIDs to label in a distinct colour.}

\item{marker.colour}{Colour for marker labels.}

\item{bins}{Number of histogram bins.}

\item{label.mode}{\code{"list"}, \code{"points"}, or \code{"both"}.}

\item{palette.name}{RColorBrewer palette name.}

\item{plot.title}{Plot title; auto-generated if \code{NULL}.}

\item{save.plot}{If \code{TRUE}, save to \code{output.file}.}

\item{output.file}{File path for saved plot.}

\item{plot.width, plot.height}{Dimensions in inches for saved plot.}

\item{include.zeros}{If \code{TRUE}, show guides absent from negative as zeros.}

\item{zero.x}{X-position for the zero bar.}

\item{zero.bar}{If \code{TRUE}, draw a capped bar for zero-count guides.}
}
\value{
A ggplot object.
}
\description{
Per-replicate histogram of (negative / pre-sorted) cell-number ratio, where
each panel shows only its corresponding positive sample's hits. For each
panel it finds the lowest-x hit, z-scores it against the ratio distribution,
and reports the fraction of negative cells at or below that threshold.
}
