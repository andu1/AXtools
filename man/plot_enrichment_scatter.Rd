% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crispr_plots.R
\name{plot_enrichment_scatter}
\alias{plot_enrichment_scatter}
\title{Enrichment scatter comparing two positive replicates}
\usage{
plot_enrichment_scatter(
  sample.info,
  pos.samples = c("pos-1", "pos-2"),
  unsorted.sample = "unsorted-1",
  id.column = "sgID",
  sample.column = "Sample_ID",
  value.column = "cell_num",
  sg.pattern = "_sg[0-9]+$",
  n.label = 20,
  log.score = TRUE,
  min.unsorted = 1,
  label.size = 3,
  point.size = 0.8,
  point.alpha = 0.35,
  palette.name = "Set1",
  plot.title = NULL,
  save.plot = FALSE,
  output.file = "enrichment_scatter.pdf",
  plot.width = 8,
  plot.height = 7
)
}
\arguments{
\item{sample.info}{Path to a CSV file or a data.frame with at least
columns for sgRNA id, sample id, and a numeric value.}

\item{pos.samples}{Character vector of exactly two positive sample names
(x-axis and y-axis).}

\item{unsorted.sample}{Name of the pre-sorted / unsorted denominator sample.}

\item{id.column}{Column name for the per-sgRNA identifier.}

\item{sample.column}{Column name holding sample names.}

\item{value.column}{Column name for the numeric value to correlate.}

\item{sg.pattern}{Regex stripped from sgID to extract the gene name.}

\item{n.label}{Number of top guides (by distance from origin) to label.}

\item{log.score}{If \code{TRUE}, plot log10(enrichment score) instead of the raw
ratio; guides with score <= 0 are dropped.}

\item{min.unsorted}{Minimum unsorted cell number to include a guide.}

\item{label.size}{Size of gene-name labels.}

\item{point.size, point.alpha}{Aesthetic parameters.}

\item{palette.name}{RColorBrewer palette name.}

\item{plot.title}{Plot title; auto-generated if \code{NULL}.}

\item{save.plot}{If \code{TRUE}, save to \code{output.file}.}

\item{output.file}{File path for saved plot.}

\item{plot.width, plot.height}{Dimensions in inches for saved plot.}
}
\value{
A list with \code{plot} (ggplot object) and \code{scores} (data.frame of all
enrichment scores).
}
\description{
Computes a per-sgRNA enrichment score (positive cell number / unsorted cell
number) independently for each of two replicates, then draws a scatter plot
comparing the two scores. The top guides by Euclidean distance from the
origin are labelled by gene name.
}
