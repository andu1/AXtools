% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crispr_plots.R
\name{plot_neg_hits_scatter}
\alias{plot_neg_hits_scatter}
\title{Faceted scatter with positive hits highlighted}
\usage{
plot_neg_hits_scatter(
  sample.info,
  x.sample = "unsorted-1",
  neg.samples = c("neg-1", "neg-2"),
  hit.map = NULL,
  spike.sample = NULL,
  id.column = "sgID",
  sample.column = "Sample_ID",
  value.column = "cell_num",
  log.transform = TRUE,
  pseudocount = 0,
  min.cells = 0,
  include.zeros = FALSE,
  zero.floor.cells = 0.5,
  cutoff.cells = 2,
  draw.cutoff = TRUE,
  border.below = TRUE,
  label.hits = "below",
  label.markers = NULL,
  base.colour = "grey75",
  palette.name = "Set1",
  point.size = 0.6,
  point.alpha = 0.3,
  hit.size = 1.9,
  label.size = 2.6,
  spike.colour = "gray35",
  spike.shape = 17,
  spike.size = 2.4,
  plot.title = NULL,
  save.plot = FALSE,
  output.file = "neg_hits_scatter.pdf",
  plot.width = 9,
  plot.height = 7
)
}
\arguments{
\item{sample.info}{Path to a CSV file or a data.frame with at least
columns for sgRNA id, sample id, and a numeric value.}

\item{x.sample}{Sample name on the x-axis.}

\item{neg.samples}{Character vector of negative sample names.}

\item{hit.map}{Named character vector mapping each negative sample to its
corresponding positive sample.}

\item{spike.sample}{Optional spike-in sample name to overlay; \code{NULL} to
disable.}

\item{id.column}{Column name for the per-sgRNA identifier.}

\item{sample.column}{Column name holding sample names.}

\item{value.column}{Column name for the numeric value to correlate.}

\item{log.transform}{If \code{TRUE}, axes are log10-transformed and regression
is fit in log space.}

\item{pseudocount}{Value added before log10 to handle zeros.}

\item{min.cells}{Minimum cell-number threshold applied to raw values in
both axes.}

\item{include.zeros}{If \code{TRUE}, guides present in \code{x.sample} but absent
from a y-sample are kept with y set to \code{zero.floor.cells}.}

\item{zero.floor.cells}{Plotted value for absent guides (used only when
\code{include.zeros = TRUE}).}

\item{cutoff.cells}{Horizontal cutoff line position in cells.}

\item{draw.cutoff}{Whether to draw the cutoff line.}

\item{border.below}{Whether to add a black ring around hits below cutoff.}

\item{label.hits}{Which hits to label: \code{"below"}, \code{"all"}, or \code{"none"}.}

\item{label.markers}{Extra sgIDs to label if present as a hit.}

\item{base.colour}{Colour for non-hit background points.}

\item{palette.name}{RColorBrewer palette name.}

\item{hit.size, label.size, spike.colour, spike.shape, spike.size}{Aesthetics.}

\item{plot.title}{Plot title; auto-generated if \code{NULL}.}

\item{save.plot}{If \code{TRUE}, save to \code{output.file}.}

\item{output.file}{File path for saved plot.}

\item{plot.width, plot.height}{Dimensions in inches for saved plot.}
}
\value{
A ggplot object.
}
\description{
Faceted scatter of a pre-sorted sample (x) vs each negative replicate (y),
with all points in grey and each panel's corresponding positive-sample hits
highlighted in colour and labelled. Correspondence defaults to suffix
matching (\code{sub("neg", "pos", ...)}). A horizontal cutoff line and black
rings around below-cutoff hits are drawn by default.
}
